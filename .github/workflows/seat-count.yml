name: TypeScript Action Workflow
on:
  workflow_dispatch:

jobs:
  run:
    name: Run Action
    runs-on: ubuntu-latest
    steps:
      - uses: softrams/activity@main
        with:
          github-token: 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
